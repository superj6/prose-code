# Short A100 run to validate the Modal path end to end.
extends: train_base.yaml
sft:
  epochs: 1
  max_steps: 20
  eval_every: 10
  save_every: 1000
  log_interval: 1
  out_dir: "/data/outputs/sft_smoke"
