# Copy to .env (gitignored). The sync service and Modal jobs both read it.
OPENAI_API_KEY=
# Optional: point at any OpenAI-compatible server (vLLM, llama.cpp) instead of api.openai.com
OPENAI_BASE_URL=
HF_TOKEN=
WANDB_API_KEY=
